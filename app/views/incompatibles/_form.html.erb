<%= form_for(@incompatible) do |f| %>
  <% if @incompatible.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@incompatible.errors.count, "error") %> prohibited this incompatible from being saved:</h2>

      <ul>
      <% @incompatible.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mate_id %><br>
    <%= f.number_field :mate_id %>
  </div>
  <div class="field">
    <%= f.label :incompatibility_id %><br>
    <%= f.number_field :incompatibility_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
